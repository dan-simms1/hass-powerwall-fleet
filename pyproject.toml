[project]
name = "hass-powerwall-fleet"
version = "0.3.0"
description = "Home Assistant integration for local Powerwall data (incl. per-PV-string detail), bootstrapped via the Tesla Fleet integration."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
